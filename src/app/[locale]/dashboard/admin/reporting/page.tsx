import { SoonPlaceholder } from "../../_shared/SoonPlaceholder";

export default function ReportingPage() {
  return (
    <SoonPlaceholder
      title="Reporting"
      pitch="Une vue financière dédiée pour suivre clics, devis en cours, ventes confirmées et commissions par canal et par campagne. Filtrage, export CSV et webhook vers votre BI."
    />
  );
}
